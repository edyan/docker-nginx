sudo: required

language: shell

services:
  - docker

env:
  - GOSS_FILES_STRATEGY=cp

script:
  - apt-get update && apt-get upgrade && apt-get install -y --no-install-recommends curl
  - curl -fsSL https://goss.rocks/install | sh
  - ./test.sh 1.10-debian
  - ./test.sh 1.15-alpine
